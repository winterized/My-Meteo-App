# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'My Meteo App' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for My Meteo App
  pod 'Alamofire', '~> 3.4'
  pod 'Gloss', '~> 0.7'

  target 'My Meteo AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'My Meteo AppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
